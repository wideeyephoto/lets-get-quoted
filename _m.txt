Fix: intake step 3 was permanently expanded, step 1 never opened

The Page tab reads as a guided 1-2-3, but step 3 was rendered bare rather
than inside a SectionCard, so it had no collapse behaviour at all. Arriving
on the tab showed the third step wide open above two shut ones, which reads
as "start here" pointing at the wrong end of the sequence.

Wrap it in a SectionCard like its siblings, and open step 1 the first time
the tab is shown so the flow starts at the top. Only on first visit — after
that the owner's own open/closed choices stand.

The panel carried its own heading and .panel frame, both of which the card
now provides; removed so it isn't a titled box inside a titled box.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
