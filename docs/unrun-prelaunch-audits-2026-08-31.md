# Unrun pre-launch audits — 2026-08-31

**Scope:** answers "what audits have NOT been run yet that are needed pre-launch?" Baseline is [docs/audit-gap-sweep-2026-08-30.md](./audit-gap-sweep-2026-08-30.md) (12 named, mostly still unrun) plus everything after the audit frontier `b5520fc4` — the AI-operator wave, the Next 14→15.5.24 upgrade, CSP report-only, the email template rewrite, and the P0 remediations — which has had **no audit of any kind**.

---

## Bottom line

One thing is on a clock: `/api/cron/contractor-lifecycle` makes its **first-ever run at 14:00 UTC on 2026-09-01**, mailing every real account owner created in the last 45 days, mid-sequence, with a day-14 email promising a dedicated phone line that no plan grants and a day-21 email selling custom domains that fail TLS. Pause it before you read anything else. Then three untested paths gate the first stranger's money: the app's own refund code ([payments.ts:861](../src/lib/payments.ts#L861), `reverse_transfer` + `refund_application_fee`) has **never executed against a live charge** — the one live refund on record was issued from the Stripe dashboard; the production database has **never been restored** from any backup; and CI has **never once built this app on Next 15**, so the annual-guarantee refund code added yesterday has not been compiled by any gate. **24 audits gate the first paying stranger, 7 clusters gate the first week, 6 can wait — and 11 of them were named a day ago in the 08-30 sweep and still have not happened.**

### If you only do six

| # | Audit | Why this one | Effort |
|---|---|---|---|
| 1 | Pause + dry-run the contractor-lifecycle cron | Fires unattended in hours; email cannot be unsent | 2h |
| 2 | Freeze a SHA, run four gates unpiped, get CI green | CI dies at unit tests; `npm run build` has never run in CI on Next 15 | half day |
| 3 | Live-mode Stripe money rehearsal (app-issued refund first) | Refunding $1,000 without the flags costs *you* $987.50 | 1–2 days, operator |
| 4 | Database + Storage restore drill | RPO and RTO are literally unknown | half day, operator |
| 5 | Failure-to-human alert drill + fix the channel | The only pager has 7 runs, 7 failures, 0 true readings | half day |
| 6 | Next-15 render-mode diff (`force-static` on the homepage) | Google Ads tag renders nothing on the apex page; CSP nonce cannot reach it | 1–2 days |

**Read this first:** another agent edits this tree live. HEAD moved from `8ae14d08` to `a7cf16a6` mid-audit, production is `e3550f58` (five commits *behind* local HEAD), and several findings below differ between committed HEAD and the working tree — flagged inline. Every audit must be run against one frozen, deployed SHA or its result means nothing. [LAUNCH_CHECKLIST.md](../LAUNCH_CHECKLIST.md) tracks this itself as `[ ] Source/Deployment Parity`.

**Tags:** `[carried]` = named in the 08-30 sweep and still unrun. `[new]` = newly needed because of work that landed after `b5520fc4`.

---

## Before the first paying stranger

### §0 — Time-boxed: before 14:00 UTC, 2026-09-01

#### 0.1 Contractor-lifecycle cron: pre-first-run dry run `[new]`
- **Not covered because:** the cron is new in this range ([vercel.json](../vercel.json), `0 14 * * *`, 35 crons now), and [LAUNCH_CHECKLIST.md](../LAUNCH_CHECKLIST.md) itself states it is "pending its first scheduled run on 2026-09-01". No dry-run flag, no preview mode exists; the only send guard is a missing `RESEND_API_KEY` at [contractor-lifecycle-emails.ts:402](../src/lib/contractor-lifecycle-emails.ts#L402), which returns *before computing anything* — so unsetting the key is not a dry run.
- **Harm:** the step loop breaks at the first unsent step whose age band matches, so a 30-day-old account receives `founder_checkin_day30` as its **first-ever** message and never sees `welcome_day0`. Both "finish your setup" nudges additionally require `alreadySent.has('welcome_day0')`, which no existing account can satisfy — so they are structurally unreachable on the first run. Three of the ten CTAs point at routes that do not exist: `/dashboard/jobs/new` (captured by the `[id]` dynamic route and rendered as a job-detail page for id `"new"`) and `/dashboard/billing` (404). Its own test pins the broken string (`expect(html).toContain(step.ctaPath)`) and will stay green forever.
- **Run it:** pause the entry in `vercel.json`. Then inject a fake Resend whose `emails.send` records and returns `{data:{id:'dry-run'}}`, run `runContractorLifecycleSweep` read-only against production, and print every `details[]` row: accountId, resolved recipient, stepId, computed `accountAgeDays`. PASS = you can name every human who would receive mail and every subject line; no test/demo account; no account gets a mid-sequence step first. Separately resolve all ten `ctaPath` values against the App Router and [next.config.mjs](../next.config.mjs) redirects.
- **Effort / who:** 2–4h. Needs the operator only to redeploy the paused `vercel.json`.

---

### §A — Before you take a stranger's money

#### A1. Live-mode Stripe money rehearsal `[carried]`
- **Not covered because:** three of ~eight money paths have run live and all three are stale or misattributed. The live $0.50 charge+refund (2026-08-17) was refunded **from the Stripe dashboard** — [go-live-2026-08-17.md:242](./go-live-2026-08-17.md) says so — exercising only the `charge.refunded` projection, never `refundPayment()`. There are four distinct `stripe.refunds.create` call sites ([payments.ts:861](../src/lib/payments.ts#L861), [stripe-direct.ts:576](../src/lib/billing/stripe-direct.ts#L576), [legacy-quick-stop-stripe-refund-executor.ts:236](../src/lib/billing/legacy-quick-stop-stripe-refund-executor.ts#L236), [subscription-cancellation.ts:407](../src/lib/billing/subscription-cancellation.ts#L407)) and **zero** have executed against a live key. The webhook route handlers were then rewritten for Next 15 (`288f7f3a`, `889da217`, `a6be05ad`) *after* both live proofs on file. The ticked live-checkout entry is also mis-dated: `sub_1U7kt1Gqh5LFKuTCJENle4Ew` appears in [subscription-cancellation-live-test-2026-08-29.md](./subscription-cancellation-live-test-2026-08-29.md), created ~08-23, presented as a fresh 08-31 verification.
- **Harm:** the comment block at [payments.ts:840-858](../src/lib/payments.ts#L840) states that without `reverse_transfer` a $1,000 refund sends $1,000 to the customer, leaves $987.50 with the contractor, and costs the platform $987.50 of its own money. The first contractor who clicks Refund is the first execution.
- **Run it:** never through the Stripe dashboard UI. (a) `stripe webhook_endpoints list --live` for **all five** receivers — only two are recorded as verified; (b) create a $0.50 live invoice, pay it, and issue the refund *from the LGQ dashboard* — PASS = Stripe shows `transfer_reversal` set and `refund_application_fee` applied, and the payments row advances by compare-and-set; (c) live top-up purchase of `crew_user` ($5) — PASS = `billing_events` scope `platform_top_up`, `projection_status='projected'`, credits granted; (d) live plan change solo→growth and back (sandbox-only today); (e) replay a captured `charge.dispute.created` against a preview and confirm `dispute_due_by` populates ([webhook/route.ts:1229](../src/app/api/stripe/webhook/route.ts#L1229)); (f) re-run every hop after any redeploy.
- **Not in scope here:** the annual 30-day guarantee build itself — already a tracked §8 launch blocker. What *is* in scope is that its refund path has never touched live money.
- **Effort / who:** 1–2 days. **Operator required** (live Stripe API).

#### A2. Database + Storage backup with a timed restore drill `[carried — the 08-30 sweep's worst finding]`
- **Not covered because:** the **backup** half was built ~4h after that sweep and genuinely runs (an out-of-repo `D:\dev-backup` rail, hourly, with `pg_dump --format=custom` and per-object Storage verification). The **restore** has never been performed. `backup.ps1` only ever calls `pg_restore --list` — a table-of-contents parse of the archive file, not a restore into any database. No `docs/restore-drill*` or `docs/backup-posture.md` exists; `git log --diff-filter=A b5520fc4..HEAD -- docs/` shows exactly one doc added in the whole frontier range. [LAUNCH_CHECKLIST.md:315](../LAUNCH_CHECKLIST.md#L315) tracks it as `[ ]` — tracked-open ≠ performed.
- **Harm:** RPO and RTO are unknown, so no honest durability statement can be made. The Supabase-flavoured archive (auth/storage/realtime/vault objects owned by `supabase_auth_admin` / `supabase_storage_admin`) is exactly the shape that fails on ownership and extension ordering into a fresh project. Meanwhile [security/page.tsx:48](../src/app/security/page.tsx#L48) tells customers there are "automated backups" and **no billing tier, PITR state or retention is recorded anywhere** — §11 records the project as `ACTIVE_HEALTHY` in us-west-2 on PG 17.6.1.141 and nothing else. Seven storage buckets hold homeowner property photos and insurance documents.
- **Run it:** (1) operator reads Supabase Settings → Billing and Database → Backups; write `docs/backup-posture.md`. (2) `pg_restore --no-owner --no-privileges --clean --if-exists` into a **throwaway** project; time it; capture every error. (3) Reconcile counts on `accounts`, `payments`, `invoices`, `quotes`, `jobs`, `auth.users` against production at dump time. (4) Point a Vercel preview at the scratch project and **log in as a real workspace member** — the only thing that proves `auth.users` plus the RLS helpers survived. (5) Restore Storage and open one job photo through the app. (6) Decrypt the offsite `secrets.tar.gz.gpg` on a clean path using the **password-manager passphrase**, not the DPAPI file (which only encrypts). PASS = a preview app serving restored data, matching counts, and a written RTO number.
- **Two fragilities found while verifying:** `backup.log` has an 8h27m gap (2026-08-30 22:18 → 08-31 06:45) with nothing alerting on a missed run; and `cloud-backup.ps1`'s GPG step defaults to a 120s timeout while the last run took 115s against a tree that grew 1.0 GB → 1.4 GB in a day — on timeout it logs "ENCRYPTION FAILED" and silently leaves the previous, stale offsite copy in place.
- **Effort / who:** half day. **Operator required** (Supabase console).

#### A3. Failure-to-human alert drill, and repair the one channel that exists `[carried]`
- **Not covered because:** no drill artifact exists anywhere, and the mechanism built for it **has never once worked**. `.github/workflows/cron-health-alert.yml` has exactly **7 runs, all `conclusion=failure`**, 2026-08-30T18:08Z → 08-31T17:26Z. The log reason: `connect ENETUNREACH 2600:1f14:...:5432` — GitHub runners have no IPv6, Supabase's direct Postgres host is IPv6-only. It has never read a single `cron_runs` row; 100% of its alerts are false. Declared cadence is `*/30`; actual is ~6 runs/day (GitHub throttles scheduled workflows), an 8× overstatement. There is no error-tracking SDK (`sentry|bugsnag|rollbar|datadog|otel` in package.json = 0 hits), no `instrumentation.ts`, no `global-error.tsx`. The four `error.tsx` files all predate the sweep and only render a message. Four of five failure sinks — `webhook_failures`, billing dead-letters, SMS delivery failures, `charge.dispute.created` — have **zero** push path; [admin-alerts.ts](../src/lib/admin-alerts.ts) is 100% read-side (`get*` only). And the fallback channel is poisoned: main's CI is red right now (run `33436652279` on `e3550f58`).
- **Harm:** `/api/cron/billing-subscription-projection` is what converts a Stripe payment into an entitlement. If it stops writing heartbeats, the job that would notice cannot reach the database, the cockpit that shows it is a page nobody opens, and CI email is permanently red — so the first paying contractor's failed entitlement grant surfaces as their support email.
- **Run it:** fix the channel *first* — a drill against a broken channel measures nothing. Point `DATABASE_URL` at the IPv4-reachable Supabase pooler (`aws-0-<region>.pooler.supabase.com`) and require a green run whose log contains a real `Summary: N OK` line. Close the untested-gate hole: `grep -c strict test/inspect-cron-health.test.ts` = **0** — the 199-line test covers interval math and never asserts the exit code, and [package.json:53](../package.json#L53) runs `inspect:cron-health` **without `--strict`**, so a hand-run cannot fail. Then the drill: manufacture one dead letter per class in Stripe test mode (a mismatched-price subscription event reproduces the never-retryable `provider_price_contract_mismatch`), start a stopwatch, record time-to-human with zero polling. **PASS = under 60 minutes, per class.**
- **Effort / who:** half day. **Operator** for the GitHub secret and mail-receipt confirmation.

#### A4. Freeze a SHA; run four gates unpiped; confirm CI has ever built Next 15 `[new]`
- **Not covered because:** `.github/workflows/ci.yml` runs Unit tests → SEO → Stock → Type check → Lint → Build, with `continue-on-error` on the security step only. CI has been failing at the **unit-test** step, so `npm run build` has **never executed in CI on a Next 15 tree**. The only green build/typecheck/lint evidence is a local snapshot; the tree has since moved five commits and still carries uncommitted edits. The upgrade is also un-isolated: `git show --numstat 288f7f3a` is 270 files carrying the leakage-calculator rewrite (+636/-308), a new `/api/tools/leakage-pdf` route, and six multi-megabyte JPEGs alongside the framework bump — **no bisect can attribute a regression to either**.
- **Harm:** `cd5c28b0` added the annual 30-day money-back Stripe refund path *after* that local snapshot. If it does not compile, the guarantee fails on the first annual customer who asks — and nothing in the pipeline would say so.
- **Run it:** delete `.next/types` before trusting typecheck (recorded failure mode). Then `npm run typecheck; echo TC=$?` / `npm test; echo TEST=$?` / `npm run lint; echo LINT=$?` / `NEXT_DIST_DIR=.next-verify npx next build; echo BUILD=$?` — each its own command, never piped. Resolve the "one non-failing edge-runtime/static-generation warning" (likely the five edge-runtime `opengraph-image` routes). Record the SHA and confirm the deployed Vercel revision equals it.
- **Effort / who:** half day. **Operator** to confirm the deployed revision.

#### A5. Production feature-flag reconciliation `[new]`
- **Not covered because:** `grep -rhoE "LGQ_[A-Z0-9_]+" src/` → **64 distinct flags**; the go-live env table lists **12**. The 52 missing include all nine durable-billing worker gates declared at [billing-worker-cron.ts:54-71](../src/lib/billing/billing-worker-cron.ts#L54), plus `LGQ_STRIPE_TOP_UP_WEBHOOK_ENABLED`, `LGQ_STRIPE_TOP_UP_PROJECTION_WORKER_ENABLED` and `LGQ_BASE_PLAN_SUBSCRIPTION_PLAN_CHANGE_ENABLED`. Every reader is `env[FLAG] === '1'`, so **absent == off, silently, with no boot-time complaint**. `ci.yml` declares zero `LGQ_*` vars, so CI only ever exercises the OFF path.
- **Harm:** narrow it to the top-up rail. `LGQ_TOP_UP_PURCHASE_ENABLED=1` is documented; the webhook and projection-worker flags are not, and no live top-up has ever been purchased. If either is absent, the first stranger to buy a top-up is charged by Stripe, [stripe-top-up-webhook.ts:38](../src/lib/billing/stripe-top-up-webhook.ts#L38) refuses the delivery before reading it, and credits are never granted — with **no failed cron and no dead letter to notice it by**. [top-up-purchases-go-live-runbook.md:48](./top-up-purchases-go-live-runbook.md) explicitly forbids this ordering.
- **Run it:** `vercel env ls production` (names, targets and `createdAt` are readable even for Sensitive vars — and **absence is the dominant failure mode**). For every flag present, confirm the newest Production deployment's `createdAt` is *later* than the variable's — Vercel bakes env at build. Then probe behaviourally: `inspect:cron-health 1440 --strict` to prove each gated worker has actually recorded a run. PASS = all 64 names are either present with a deliberate value or on a written "intentionally absent = off" list.
- **Effort / who:** half day. **Operator required.**

#### A6. Incident runbooks, a named on-call human, and a rehearsed rollback `[rollback carried; runbooks new]`
- **Not covered because:** `find . -iname "*runbook*"` returns four docs, all *feature go-live* procedures. There is no document for "the site is down", "Stripe webhooks are failing", "the database is unreachable", or "we charged someone wrongly". `grep -rniE "on-call|escalation contact|pager|incident commander"` across docs/, LAUNCH_CHECKLIST.md and README.md returns **no phone number, no ladder, no named human**. The only `## Rollback` heading in an operational doc is an AI-Voice flag matrix. **0 of 246 migrations carry a down script**, and nothing enforces the rule stated at [payment-integration-handoff-2026-08-16.md:440](./payment-integration-handoff-2026-08-16.md) that the previous deployment must still process every row the newer schema creates. Both drills are tracked `[ ]` in §12.
- **Harm:** every Vercel action is human-relayed through the Codex operator, so "roll back now" currently has prose-composition latency at the exact moment money is failing. Two single commits in the unaudited range have app-wide blast radius: the Next 15 major and CSP-to-report-only.
- **Run it:** four one-page runbooks in `docs/runbooks/` (site-down, stripe-webhooks-failing, database-unreachable, wrong-charge-makegood), each naming: the console to open, the read-only command that confirms the diagnosis, the kill switch and its **exact flag name**, and who to notify. Plus `on-call.md` naming one human and a backup. Then **rehearse the rollback once**: identify the previous-good deployment, execute the instant rollback, curl `/`, `/pricing`, `/login`, `/api/health`, roll forward, and record the elapsed time. The kill-switch table must state that a Production flag change is an **ADD, not an edit**, and does nothing until redeploy. PASS = someone who has never read this thread can roll back Production from the doc alone.
- **Effort / who:** half day. **Operator** for the rollback leg.

#### A7. Support reachability and chargeback-evidence ownership `[carried, partial]`
- **Not covered because:** no probe result exists anywhere. `support@letsgetquoted.com` is named in **8 customer-visible strings** — including [user-manual.ts:1305](../src/lib/help/user-manual.ts#L1305), which tells contractors that emailing dispute evidence there before `dispute_due_by` is "The ONLY Correct Action" — and **no code in this repo ever sends to or from it**; every actual send goes to `hello@`. `/compare/[competitor]` publicly promises "free migration assistance within 24 hours". The in-app rail is gated on `requireOfficeContext('leads.read')` ([help/actions.ts:37](../src/app/dashboard/help/actions.ts#L37)), so a locked-out, suspended or payment-failed contractor **cannot open a ticket**, and homeowners paying an invoice have no channel at all. On disputes, [webhook/route.ts:1203](../src/app/api/stripe/webhook/route.ts#L1203) states the platform is `losses_collector` — a lost dispute is the platform's money — and the code emails only the **contractor**.
- **Run it:** send an external probe to both addresses and time the human reply. Write `docs/runbooks/chargeback-evidence.md` naming the owner, the deadline source (`payments.dispute_due_by`), and the exact queries that produce the evidence pack (quote, invoice, signed acceptance, arrival timestamps, SMS thread). Dry-run it against one historical payment in under 30 minutes. Decide and publish the logged-out support path on `/login` and the invoice-pay page.
- **Effort / who:** hours. **Operator/human** (mailbox access).

---

### §B — Before a stranger can sign up at all

#### B1. Email deliverability and Resend bounce-webhook liveness `[carried]`
- **Not covered because:** no message from this domain has ever been observed landing in a real Gmail, Outlook, Yahoo or iCloud inbox; no header has ever been read for SPF/DKIM/DMARC alignment. `git log --all --grep='inbox placement|mail-tester|dmarc'` → zero commits. Worse, the ticked DNS line is a **name substitution**: Resend asserts *its own MAIL FROM subdomain* `send.letsgetquoted.com` is configured, while the **root** `letsgetquoted.com` TXT contains only `MS=`, `google-site-verification=`, `twilio-domain-verification=` — **no `v=spf1` at all** — and every customer-facing From is `hello@letsgetquoted.com` ([email.ts:367, 1210, 1250, 1283, 1337](../src/lib/email.ts#L367)). DMARC is `p=none`. `RESEND_WEBHOOK_SECRET` is absent from the master env table, and [resend/webhook/route.ts:71-88](../src/app/api/resend/webhook/route.ts#L71) **500s every event** when it is unset, with a comment naming this "the likeliest webhook failure there is". Every template was rewritten on 2026-08-31 by `f8969fb0` — the magic-link body went from a plain div to a full branded template and the From header changed — so any earlier measurement is void.
- **Harm:** the magic link is the only login path for a new contractor. If it lands in Spam, the first paying stranger completes Stripe checkout and then cannot get back into the product.
- **Run it, in this order:** (1) **30 min, cheapest and most likely broken** — `SELECT status, count(*) FROM email_events WHERE occurred_at > now() - interval '14 days' GROUP BY 1` and `SELECT count(*) FROM webhook_failures WHERE source='resend' AND resolved_at IS NULL`. Empty `email_events` = the endpoint is not registered and the entire suppression rail has never run. (2) 10 min, no operator: `nslookup -type=TXT letsgetquoted.com`. (3) 2h seed test across four real inboxes for magic link, invoice **with PDF**, quote, campaign, review request — record Inbox vs Spam vs Promotions, require SPF/DKIM/DMARC PASS in "Show original", open the PDF ([email.ts](../src/lib/email.ts) swallows PDF-generation failure and sends attachment-less), and confirm one-click Unsubscribe writes a suppression row. (4) mail-tester, PASS ≥ 9/10. (5) Add root `v=spf1 include:_spf.google.com ~all`; confirm a human can read `dmarc@`; enrol in Postmaster Tools; `p=none` → `p=quarantine` only after 2–4 clean weeks.
- **Effort / who:** half day. **Human with four inboxes + operator** for Resend/DNS.

#### B2. Clean-slate onboarding walkthrough `[carried]`
- **Not covered because:** zero hits for `clean-slate|onboarding walkthrough` outside the sweep's own recommendation. [TEST_PLAN.md:225](../TEST_PLAN.md) records the opposite: "Email sending: not automated — no inbox access". The checklist has no onboarding, signup or first-run item in any of sections 0–12. And `b783bfce` added a **mandatory homeowner contact-preference field** to the intake path while `288f7f3a`/`e152015c` moved the whole app to async API contracts — so even a pre-frontier walk is stale.
- **Harm:** the composition — signup → terms → Stripe Connect to charges-enabled → first quote → send → homeowner pays from a phone — is exercised by no unit test and no CI gate. A signup that dead-ends produces no error anyone will see.
- **Run it:** production, cookieless profile, external mailbox nobody on the team owns, stopwatch running. Complete Connect onboarding, send a quote to a second controlled email + phone, open it **on a real phone over cellular** (not desktop emulation), pay $1 with a real card, refund it from the dashboard. Record total minutes, every email that landed *and every one that did not*, every wrong default, and the exact copy of any error. Repeat once entering via `/pricing?plan=solo`. PASS = a stranger reaches "quote sent" in under 15 minutes with no support contact. **The written doc is the artifact.**
- **Effort / who:** half day. **Human on production.**

#### B3. Route and server-action authorization manifest, with a gate that bites `[new]`
- **Not covered because:** the gate that exists is theater. [test/api-route-posture-audit.test.ts:40](../test/api-route-posture-audit.test.ts#L40) is `isCron || isWebhook || isAuthGuarded || isRateLimited || isSignedTokenGuarded || isPublicUtility || isReExport` — seven OR'd **substring** probes. `isWebhook` passes on `rel.includes('webhook')` alone (four Stripe/permit routes pass on their *path*, matching no signature substring). `isPublicUtility` is a bare directory allowlist. `isRateLimited` includes the dead alternative `content.includes('rateLimit')` — a symbol that appears in **zero** API routes. `isReExport` passes on an import string. The floor is `toBeGreaterThan(10)`. It walks only `src/app/api` (128 of 142 handlers) and covers **zero of ~480 server actions** across 93 `'use server'` files.
- **Harm, already realised:** [admin/accounts/[id]/export/route.ts](../src/app/admin/accounts/[id]/export/route.ts) — a GET assembling a **26-table full-tenant export** (accounts, clients, leads, jobs, invoices, payments, sms_messages, sms_consent…) with `createAdminClient()` and **no authentication** — shipped `cafe1431` (08-30) and was fixed 31 hours later by chance. It lives outside `src/app/api`, so the green test never saw it. **Still open at committed HEAD:** [admin/operator/actions.ts](../src/app/admin/operator/actions.ts) exports four `'use server'` actions, three minting a service-role client, with **no** `requireAdmin`/`requirePermission`/`requireMfaPermission`; `grep -n 'admin' src/middleware.ts` returns nothing, so the per-action check is the only layer. A fix exists **only in an uncommitted working tree** — production runs the unguarded version. Next.js emits server-action ids into client bundles, so an unguarded action is a public HTTP endpoint by construction — [auth.ts:806](../src/lib/auth.ts#L806) states this rule verbatim.
- **Run it:** check in a manifest with one row per handler *and per exported action*, declaring exactly one posture from a closed set. Rewrite the test to walk **all** of `src/app`, fail on anything absent from the manifest, and — for anything claiming a guard — **assert the guard is invoked** by parsing each exported function body and requiring a `requireX(` call expression (the technique already used correctly in [test/payment-actions-are-guarded.test.ts:55](../test/payment-actions-are-guarded.test.ts#L55)). Add the invariant: any body containing `createAdminClient(` must also contain a guard call unless its row says `intentionally-open`. Delete the `includes('webhook')` and directory rules. **Prove it bites** with a scratch probe route, including under `tools/`. Then POST each of the four operator action ids with no cookie against a preview — PASS = 404/403, no audit row, no JSON.
- **Effort / who:** days. Repo-only.

#### B4. Abuse-surface allowlist, phone OTP, and the worst-case 24-hour bill `[allowlist carried; cost ceiling new]`
- **Not covered because:** `find src/app/api -name route.ts` = **128**; `grep -rl checkRateLimit` = **13**. There is no allowlist file. Only 19 of 93 action files reference a limiter. Phone OTP is **client-side**: [login/page.tsx:150](../src/app/login/page.tsx#L150) calls `supabase.auth.signInWithOtp({phone, channel:'sms'})` inside a `'use client'` component — the browser talks straight to Supabase Auth, so **no app-side limiter is in the path**, while the email magic link *is* protected ([login/actions.ts:35](../src/app/login/actions.ts#L35), per-email + per-IP `checkRateLimitStrict`). The ad-billing 2FA verify action has no attempt counter (900k-guess brute force) and no server-side send throttle — raised 2026-08-31, no remediation commit. `checkRateLimit` **fails open** ([rate-limit.ts:30](../src/lib/rate-limit.ts#L30)). The AI Operator's Gemini calls bypass the metered path entirely ([ai-operator/engine.ts:128](../src/lib/ai-operator/engine.ts#L128) constructs `GoogleGenAI` directly). No spend figure exists anywhere in the repo.
- **Harm:** the two cheapest attack paths are the two with no app-side limiter at all, and both bill a personal card. The green test is itself the harm — it converts an open question into an apparent "verified".
- **Run it:** replace the boolean-OR with a checked-in allowlist mapping all 128 routes **and** all 93 action files to `{rateLimited: <bucket,limit,window>}` or `{exempt: '<reason>'}`; fail closed on new files; parse with an AST, never `content.includes`. Move phone OTP to a server action mirroring the email path. Add an attempt counter to the 2FA verify. Then compute the arithmetic: for OTP SMS, outbound SMS, AI writing/vision, PDF render and Maps loads, multiply per-call cost by max requests/hour under the limiter that actually applies → `docs/cost-ceiling.md`. Snapshot the Supabase Auth SMS caps (console-only) and set a hard budget or alert on every provider.
- **Effort / who:** days. **Operator** for the provider consoles.

#### B5. Homeowner mobile matrix on the token surfaces `[carried]`
- **Not covered because:** nothing in this repo has ever rendered `/pay` at a phone width. The only phone-viewport script touching token surfaces is `_brandmobile.mjs` — **untracked** (`git ls-files "_*.mjs"` is empty), mtime **2026-08-05**, covering 2 of 6 classes (`/invoice`, `/portal/view`), output never recorded, one `git clean` from gone. No `playwright.config.*` exists anywhere; playwright is a bare devDependency. `grep -rn "devices\["` across the tree = **zero** — a narrow chromium viewport is not mobile Safari and will not reproduce iOS viewport units, the Safari bottom bar, or Apple Pay availability.
- **Harm:** `/pay` takes the money and its real user is mobile Safari opened from an SMS tap. If the Stripe Payment Element or Apple Pay button overflows or is clipped by the sticky brand bar at 320–390px, the homeowner cannot pay — and it fails in front of the contractor's own customer on their first invoice.
- **Run it:** rescue the script into `scripts/`, extend ROUTES to six seeded token classes, replace viewport-only contexts with `browser.newContext({ ...devices['iPhone 14'] })`, and add `/pay`-specific assertions: Stripe iframe `boundingBox().width <= viewport`, submit height ≥ 44, and `elementFromPoint` at its centre returns the button (not the sticky bar). PASS = zero FAIL lines across 6 routes × 3 devices, screenshots retained, non-zero exit on failure — **do not pipe it**.
- **Effort / who:** half day. Repo-only.

---

### §C — Before you spend a dollar on acquisition

#### C1. Next-15 render-mode diff: which routes prerender, and what reads an empty request context `[new — the strongest single finding]`
- **Not covered because:** no upgrade artifact exists; the only record is a commit message and two checklist lines, and the post-sweep audit never mentions CSP, nonce or `force-static` despite the causing commit landing inside its range.
- **Mechanism, verified in the installed framework:** [next/dist/server/request/headers.js:33-38](../node_modules/next/dist/server/request/headers.js#L33) — under `workStore.forceStatic`, `headers()` returns an **empty object without tracking**; `cookies.js` is identical. [src/app/page.tsx:5-6](../src/app/page.tsx#L5) sets `dynamic = 'force-static'` (the app's **only** force-static segment), added by `d8d7b116`, a *performance* commit. Three consumers on that route degrade silently: (1) [csp-nonce.ts](../src/lib/csp-nonce.ts) reads `headers()` inside a try/catch → nonce `undefined`, and **the catch swallows the `DynamicServerError` Next throws to opt a route into dynamic rendering**; (2) [google-tag.tsx:53](../src/components/google-tag.tsx#L53) reads `x-pathname` → null → `isSensitivePath(null)` returns true ("fail closed") → **renders nothing**; (3) [layout.tsx:145](../src/app/layout.tsx#L145) `readServerTheme()` ignores the visitor's cookie. `x-pathname` has exactly one producer, [middleware.ts:102](../src/middleware.ts#L102), set **after** the subdomain early-return — so every tenant subdomain independently reproduces the same bug.
- **Harm:** the Google Ads conversion tag `AW-18400954668` renders nothing on the apex homepage, so **the first paid-acquisition spend is untracked and Google's bidder gets no signal**. The CSP nonce cannot reach prerendered HTML, which is the actual cause of the site-wide CSP revert one day later — the checklist's own measurement is **0 of 39 homepage script tags nonced, 2 of 35 on /contact**, and `/contact` has no segment config at all. No test covers any of it: [test/google-tag.test.ts](../test/google-tag.test.ts) asserts source substrings and never renders.
- **Run it:** `NEXT_DIST_DIR=<scratch> npx next build` (unpiped; capture with `> out.txt 2>&1; echo $?`). Read `prerender-manifest.json`, build a Static/ISR/Dynamic table for every route, and **diff it against the same table from `6e128eed`** (pre-upgrade) in the detached worktree — every changed verdict is a finding. Grep for the swallow pattern: any `await headers()`/`await cookies()` inside a try/catch is a route that can prerender with an empty request context. Then prove it live: `curl -s https://letsgetquoted.com/ | grep -o 'nonce="[^"]*"' | wc -l` and `grep -c googletagmanager`, repeated for `/contact`, `/pricing`, `/faq`, `/features`, `/resources`.
- **Effort / who:** days. Repo-only.

#### C2. Sold-vs-built claim sweep across every customer-facing surface `[new]`
- **Not covered because:** three real guards exist and all three hardcode the same three files — one of which, `PricingCalculator.tsx`, **has zero importers** (dead code), so a third of the guarded surface is unreachable. Worse, the dedicated-number guard iterates `/'([^'\n]{0,300})'/g` and the offending FAQ answer is **~430 characters**, so the guard is *structurally blind* to the entire FAQ block.
- **Harm:** [pricing-catalog.ts:232](../src/app/pricing/pricing-catalog.ts#L232) ships, live today, "Yes, AI Voice is available! … you and your crew can dial in using your voice credits **immediately** on our shared number" — for three SKUs that [catalog.ts:420](../src/lib/billing/catalog.ts#L420) withholds because "no live Price exists, no number is pointed at the route, and nothing grants voice_minutes". Outside the guarded files: [changelog.ts:175](../src/lib/changelog.ts#L175) "Provision dedicated local business phone numbers in minutes" (`dedicatedBusinessNumbers` is **0 on all four plans**); [features/ai-voice/page.tsx](../src/app/features/ai-voice/page.tsx) sells a 24/7 receptionist in the present tense with zero caveat and zero test coverage; [PlanUsageSection.tsx:167-171](../src/app/dashboard/settings/PlanUsageSection.tsx#L167) renders AI Voice rows on the live Plan & usage tab. This file has been inverted before — the post-sweep audit found `VOICE_PURCHASABLE` flipped to true with four negative tests deleted.
- **Run it:** replace the hardcoded source list with a **derived** surface set (every public-route `.tsx`, plus `changelog/features/all-features-catalog/resources.ts`, plus `src/emails/**`, plus `PlanUsageSection.tsx`). For each capability whose sellability is expressed in code (`VOICE_PURCHASABLE`, `SELLABLE_TOP_UP_IDS`, `TOP_UPS_WITHHELD`, `allowances.dedicatedBusinessNumbers`, `customDomainConnections`), assert no string literal in any derived surface makes a present-tense claim about a withheld or zero capability. Require an explicit per-file allowlist entry with a reason. **PASS = `npm test` goes red today on `pricing-catalog.ts:232` and `changelog.ts:175`.**
- **Effort / who:** days. Repo-only.

#### C3. Advertising substantiation (FTC Act §5) `[new]`
- **Not covered because:** no substantiation file, claims register or citation list exists anywhere; grep for `substantiat|claims register|FTC` across docs/ and the checklist returns nothing.
- **Harm — three claims with no possible basis:** [platform-campaign-templates.ts:194](../src/lib/platform-campaign-templates.ts#L194) "guarantees **100% carrier delivery rates**" (unachievable on any carrier network); [TradeInsuranceClaimsShowcase.tsx:116](../src/app/for/[trade]/TradeInsuranceClaimsShowcase.tsx#L116) "keeping your business **100% UPPA compliant**" on a live indexed page, plus a "UPPA Compliant" badge at :39; and [platform-campaign-templates.ts:164](../src/lib/platform-campaign-templates.ts#L164) "**When we analyzed the contractors with the highest win rates on Let's Get Quoted**, three clear patterns stood out" — a proof claim from a product with **zero customers**. Commit `08cdd98a` softened four *other* strings on that exact showcase file two days ago and left both 100% claims untouched, which proves the copy is edited without a claims pass. Also unsourced: "closes 30% more jobs", "2.8x more likely to be approved", "increase average invoice size by 22%".
- **Run it:** `grep -rnE '[0-9]+%|[0-9]+x |guarantee|100%|compliant|proven|average'` over the campaign templates, `/for`, `/features`, `/pricing`, `/compare`; for each hit record claim text, file:line, express-vs-implied, and the evidence. Every quantified or comparative claim needs a named third-party citation or platform data that exists; every `guarantee`/`100%`/`compliant` gets removed or qualified. PASS = a committed `docs/claims-substantiation.md` where every row has a source, plus a standing test that fails on an unregistered percentage/guarantee string.
- **Note:** the four quantitative *plan* claims (10 crew seats, 1,500 SMS credits, 0.25% fee, 1 custom domain) were checked and **match `BILLING_PLANS` exactly** — those need no change.
- **Effort / who:** days. Repo + a legal read on "UPPA".

#### C4. Outbound-email compliance invariant: List-Unsubscribe ↔ footer ↔ suppression, pinned to every send site `[carried, and the surface has tripled since]`
- **Not covered because:** `grep -rn "List-Unsubscribe" test/` returns **zero hits across 857 test files**. There are now **three independent copy-pasted implementations** of the same two primitives — [email.ts:83](../src/lib/email.ts#L83), [contractor-lifecycle-emails.ts:216](../src/lib/contractor-lifecycle-emails.ts#L216), [admin-platform-campaigns.ts:42](../src/lib/admin-platform-campaigns.ts#L42) — the latter two added inside the unaudited range.
- **Harm 1 — fail-open suppression, newly introduced:** [email-suppression.ts:76-110](../src/lib/email-suppression.ts#L76) was deliberately written to **fail closed** on a database error. Neither new sender uses it. [contractor-lifecycle-emails.ts:464](../src/lib/contractor-lifecycle-emails.ts#L464) and [admin-platform-campaigns.ts:188](../src/lib/admin-platform-campaigns.ts#L188) both do `const { data: suppressions } = await admin.from('email_suppression')…` with the `error` destructure **omitted** — the second's comment literally says "fail closed on opted out emails". On a query error the Set is empty and **the batch mails everyone who opted out**, from the same domain that carries every invoice and magic link.
- **Harm 2 — wrong legal entity:** [contractor-lifecycle-emails.ts:231](../src/lib/contractor-lifecycle-emails.ts#L231) and [admin-platform-campaigns.ts:57](../src/lib/admin-platform-campaigns.ts#L57) both read `process.env.COMPANY_MAILING_ADDRESS || "Let's Get Quoted Inc. · Austin, TX"` — not a valid postal address, and [terms.ts:1](../src/lib/terms.ts#L1) declares the entity is **LETS GET QUOTED LLC**. If the var is unset in Production that is a CAN-SPAM address defect *plus* a false statement of legal identity in the first commercial mail a customer receives. **And if it IS set**, [email-suppression.ts:163](../src/lib/email-suppression.ts#L163) makes the same fallback available to *contractor* campaigns, so the block at [marketing/actions.ts:345](../src/app/dashboard/marketing/actions.ts#L345) **can never fire** — a contractor with no address of their own silently mails under LGQ's address as if it were theirs. Two further call sites bypass that check entirely.
- **Run it:** fix the two fail-open reads before the lifecycle cron's first run. Delete the address fallback so a missing var fails loudly. Then write the invariant test the 08-30 sweep specified: walk every `resend.emails.send(`/`sendEmail(` call site, require an explicit TRANSACTIONAL/MARKETING map entry, and for MARKETING assert **at the call site** a headers object with both `List-Unsubscribe` and `List-Unsubscribe-Post`, a footer with a non-empty postal address, and a suppression consultation. Add a case asserting a *throwing* suppression query blocks the send. PASS = the test goes red when you delete the headers argument from any one of the five marketing call sites. Also send one real campaign to a controlled inbox and read the raw source.
- **Effort / who:** half day. **Operator** to read `COMPANY_MAILING_ADDRESS` from Production.

---

### §D — Before a homeowner touches a token surface

#### D1. Supabase Realtime channel authorization — crew GPS broadcast `[new, unknowable from repo]`
- **Not covered because:** `grep -rn "private: true" src/` returns **ZERO hits**. [field/location/route.ts:132](../src/app/api/field/location/route.ts#L132) publishes to `account:${accountId}:crew-locations` with a comment at :129 reading "Realtime Broadcast notification on **private topic**" — while neither publisher nor subscriber ([LiveCrewMap.tsx:191](../src/app/dashboard/crew/LiveCrewMap.tsx#L191)) passes `{ config: { private: true } }`. This is the repo's signature failure shape: a comment vouching for a property the code does not have. `grep -rn 'supabase_realtime|realtime.messages|publication' migrations/ schema.sql` returns nothing, so the authorization posture exists **only in the Supabase console** and is not under version control. Two further subscriptions ([JobsWorkspace.tsx:192](../src/app/dashboard/jobs/JobsWorkspace.tsx#L192), [LeadsWorkspace.tsx:268](../src/app/dashboard/leads/LeadsWorkspace.tsx#L268)) use `postgres_changes` on `public.jobs` with **no `filter:`** — the whole table, every tenant.
- **Harm:** if the project does not enforce private channels, anyone holding the anon key (shipped in every page bundle) plus one account UUID subscribes and receives **continuous live GPS of another contractor's named employees**, with lat/lng/heading/speed. That is the worst confidentiality outcome available in the product, decided by one console setting.
- **Run it:** from a browser with **only** the public anon key and no session, `createClient(URL, ANON).channel('account:<other-tenant-uuid>:crew-locations').on('broadcast',{event:'location_update'},console.log).subscribe()` while a crew member on that account is clocked in. **PASS = nothing arrives and subscribe errors with an authorization failure.** Repeat for `postgres_changes` on `public.jobs` as a signed-in user of account A. Then enforce it in code (`private: true` both sides + `realtime.messages` RLS keyed on the account in the topic name) and add a test asserting every `.channel(` call site passes it.
- **Effort / who:** half day. **Operator** (console) + a throwaway browser.

#### D2. Storage bucket tenancy `[new, unknowable from repo]`
- **Not covered because:** seven buckets are referenced from `src/`; only **two** have a `storage.buckets` row in version control. `grep -rn "storage.objects" migrations/ | grep -i policy` returns **NOTHING** — there are **zero storage RLS policies anywhere in the repository**. Object paths are `${accountId}/${randomUUID()}.ext`, i.e. the account id is the first path segment — which is only a boundary if a policy enforces it. The public/private flag for `job-photos`, `lead-photos` and `crew-photos` exists only in the console. (`site-images` and `site-videos` are deliberately public — the code uses `getPublicUrl`.)
- **Harm:** nobody can answer "can contractor A fetch contractor B's job photos". If any private-intended bucket is public, every homeowner property photo is fetchable by URL with no credential. *Likelihood is lower than it looks* — the app never touches Storage from the browser (`grep -rln 'storage.from(' src/ --include=*.tsx` = nothing; every read goes through `createSignedUrl` server-side) so this needs a hand-crafted anon-key request. That lowers the odds, not the exposure.
- **Run it:** `select id, name, public from storage.buckets` and `select * from pg_policies where schemaname='storage'`. Then the live probe that actually decides it: upload to `job-photos` under account A; curl its public URL with no credentials; from a session belonging to account B, `list('<accountA-uuid>')`, `.download(path)`, and `.upload()` into A's prefix. PASS = 400/404, empty list **and** download refused, upload refused. Then codify all seven buckets and `(storage.foldername(name))[1] = account_id` policies in a migration, so the answer stops living in a console.
- **Effort / who:** half day. **Operator required.**

#### D3. Tenant scoping of the ~400 service-role call sites `[new]`
- **Not covered because:** `grep -rn "createAdminClient(" src/` = **400 call sites across 224 files**; the session client is 53. So **~88% of database access bypasses RLS entirely**. The post-sweep audit measured that **62 of 161 public tables are RLS-enabled-with-no-policy** — for those, the only thing between tenant A and tenant B is a hand-written `.eq('account_id', …)` in TypeScript. The one shipped isolation prover, [scripts/verify-tenant-isolation.mjs](../scripts/verify-tenant-isolation.mjs), does `set local role authenticated` — it exercises the **session** path exclusively and **cannot observe a service-role query at all**. No script, test or npm target audits service-role scoping; `test/payment-actions-are-guarded.test.ts` does it correctly for exactly one file.
- **Harm:** a single omission is a silent cross-tenant read or write with no error — and this repo has already shipped that class three times, including cross-tenant SMS billed to the victim. RLS refusals return no error, so a failed boundary and a successful one look identical to the calling code.
- **Run it:** dump the 400 sites; classify each following query chain as (a) scoped — a literal `.eq('account_id', X)` where X **provably derives from a guard return value**, (b) unscoped-by-design, (c) **scoped from caller input** — the failure class. Check in a manifest, one line per site, and add a test that re-derives the inventory and fails on any site not in it. **Do not accept "the guard is imported in this file" as scoping** — assert the account-id *value* flows from the guard. PASS = zero sites in class (c).
- **Effort / who:** days. Repo-only.

#### D4. Token-surface inventory: entropy, expiry, single-use, secret coupling `[partly carried — the sweep covered magic links only]`
- **Not covered because:** no document treats tokens as a set. Ten classes are minted in five modules with no shared helper. Two holes are visible from the code alone: [crew-verification.ts:63](../src/lib/crew-verification.ts#L63) mints the 6-digit phone-verification code with `Math.floor(100000 + Math.random() * 900000)` — a **non-cryptographic PRNG**, the only `Math.random()` in any token/OTP file; and [reviews.ts](../src/lib/reviews.ts) tokens have **neither expiry nor `used_at`** (grep for `expires|used_at|revoked` returns only the import), making each a permanent handle to a customer relationship. A third is latent and self-inflicted: **five** token/HMAC secrets fall back to `SUPABASE_SERVICE_ROLE_KEY` when their own env var is unset ([estimate-continuation-token.ts:17](../src/lib/estimate-continuation-token.ts#L17), [email-suppression.ts:18](../src/lib/email-suppression.ts#L18), [quickbooks/state.ts:22](../src/lib/quickbooks/state.ts#L22), [subcontractor-dispatch.ts:643](../src/lib/subcontractor-dispatch.ts#L643), [voice/auth.ts:177](../src/lib/voice/auth.ts#L177)) — plus `account-closure-orchestrator.ts`, which additionally falls back to a hardcoded literal.
- **Harm:** rotating the Supabase service-role key would silently invalidate every unsubscribe link, QuickBooks OAuth state, subcontractor offer token and voice auth signature **in the field** — discovered during the incident the rotation was meant to resolve.
- **Run it:** build the table (who mints, what RNG, how many bits, storage, expiry column + enforcement site, single-use column + enforcement site, what it authorizes, rate limit). Consolidate to one `mintCapabilityToken()` using `randomBytes ≥ 24`, and add a test failing on any `Math.random()` reachable from a file matching `/token|otp|verification|invite/`. Empirically: request 200 crew OTPs and test for predictability; replay one expired and one used token of each class; fire 1,000 near-miss ids at `/pay/[id]`, `/invoice/[id]`, `/api/invoices/[id]/pdf` and confirm indistinguishable 404s and a limiter that trips. Make the four remaining secret fallbacks fail closed in production, as `estimate-continuation-token.ts:19` already does.
- **Effort / who:** days. Repo-only.

#### D5. TCPA quiet-hours conformance and SMS consent provenance `[new]`
- **Not covered because:** no legal/compliance audit exists in any of the 47 docs/ files. `grep -rn 'isWithinTcpaQuietHours|getTcpaCompliantSendTime' src/` returns **two** enforcement sites outside the helper — [ad-speed-to-lead.ts:155](../src/lib/ad-speed-to-lead.ts#L155) and [sms.ts:1839](../src/lib/sms.ts#L1839) (intake confirmation only) — against **44** `export async function send*` in `sms.ts`. Campaign blasts, review requests, quote follow-ups, rebook invites and appointment reminders all route straight to `queueAccountSms`; `campaigns.ts` has no hour logic at all. Reminders evaluate the hour in the **account's** timezone ([appointment-reminders.ts:16](../src/lib/appointment-reminders.ts#L16)), `REMINDER_HOUR_CHOICES` offers **6 and 7 AM** — below the promised floor — and a 3-hour catch-up lets a 20:00 selection fire at 22:59. `52a9dc29` added unit tests for the predicate, which makes the *unenforced* paths look covered.
- **Harm:** [sms-terms/page.tsx:133](../src/app/sms-terms/page.tsx#L133) promises 8 AM–9 PM **at the recipient's location**, citing 47 C.F.R. § 64.1200(c)(1) — a citation `51abfa53` *strengthened* on 2026-08-31 without adding a single enforcement call site. "The defendant's own website said it did X", at $500–$1,500 per message, on a channel that is bulk by definition. The first contractor who imports a coast-spanning list triggers it.
- **Consent, separately:** [sms.ts:658](../src/lib/sms.ts#L658) `recordSmsConsent` writes `status:'opted_in'` unconditionally, and six call sites fire it as a **side effect of the contractor deciding to send** with no consent field — [jobs/actions.ts:965](../src/app/dashboard/jobs/actions.ts#L965) (job update), :1568 (review request), [reviews.ts:294](../src/lib/reviews.ts#L294), [scheduling.ts:148](../src/lib/scheduling.ts#L148), and the two `if (sendSms && homeownerPhone)` sites. The file's own comment at :744 says the app "may not manufacture it merely because an owner typed a phone number". A ledger that generates its own consent is worthless as a defence and affirmatively bad as a discovery artifact. (Several *other* sites do gate on a contractor attestation, and the public intake form now records a versioned `intake_v1_2026` consent — the gap is the dashboard side.) Separately, **nothing in `src/` or `migrations/` ever writes any of the five `AFFIRMATIVE_MARKETING_SOURCES`**, so the sold campaign-SMS audience is structurally zero.
- **Run it:** walk every exported `send*` and require an entry in `QUIET_HOURS_EXEMPT` (with a written reason) or `QUIET_HOURS_ENFORCED` — and for the latter assert the module **calls** the predicate, not merely imports it. For every enforced path prove the hold is durable by querying the table the held message lands in. Classify every `recordSmsConsent` call site as genuine capture / baseline registration / manufactured. Then diff the result against the exact sentences in `/sms-terms` and `/terms`. PASS = every sentence is true of every path, or the sentence is narrowed.
- **Effort / who:** days. Repo-only (a legal read on the outcome is wise).

#### D6. Legal instruments and regulated-conduct surfaces `[new]`
Four separate items, grouped because they share one remedy shape: *decide the jurisdictions you serve, gate on state, or stop generating the artifact.*
- **Lien waivers / NOI:** [noi-generator.ts](../src/lib/noi-generator.ts) has **zero** state/jurisdiction references yet emits "STATUTORY NOTICE OF INTENT TO FILE MECHANIC'S LIEN" with a hardcoded 10-day cure default and threatens "recording and filing a verified Mechanic's Lien… initiating legal foreclosure proceedings". [lien-waiver.ts:70,98](../src/lib/lien-waiver.ts#L70) carries recognisably **California statutory** language with no state parameter. Pre-lien notice content and deadlines are entirely state-specific; a contractor who relies on this can forfeit lien rights, and a lien threat without underlying rights is itself actionable. **Most dangerous single line in the batch:** [PaymentModals.tsx:2570-2580](../src/app/dashboard/payments/PaymentModals.tsx#L2570) — the "Log Tracking" button for the USPS certified-mail number, the *only* proof-of-service artifact that matters if the notice is litigated, calls `onSuccess(...)` and **persists nothing**. No server action, no table.
- **Insurance supplementing:** `51abfa53` did real work (removed the false inspection attestation, added a disclaimer, moved fictional defaults behind a sample loader). What it did not address: the artifact is still a **carrier-addressed demand** ("Insurance Carrier:", "Attn: {adjusterName}", "Total Supplement Amount Requested") whose dollar figures are per-trade **hardcoded `defaultEstimatedCost` constants**, not a measured scope, and [insurance-claims.ts](../src/lib/insurance-claims.ts) has **no state parameter anywhere**. Several states treat contractor scope negotiation with a carrier on a homeowner's claim as unlicensed public adjusting; a footer disclaimer does not change the conduct. "UPPA" survives at eight code sites and two public-page sites and is resolved to no statute or model act anywhere.
- **Card surcharge modal:** [PaymentModals.tsx:2481](../src/app/dashboard/payments/PaymentModals.tsx#L2481) tells contractors surcharging is "compliant nationwide" — it is prohibited outright in at least Connecticut, Massachusetts and Puerto Rico — and :2483 promises "Let's Get Quoted **automatically displays** clear itemized fee disclosure before checkout" when a repo-wide grep finds **no surcharge route, table, column or receipt line anywhere**. The "Apply Policy to Checkout" button persists nothing. **The launch action here is almost certainly deletion, not an audit** — minutes of work, and the false legal guidance is live in the dashboard today.
- **Terms versioning:** [terms.ts:11](../src/lib/terms.ts#L11) pins `TERMS_VERSION = '2026-08-29'` and that string is written into acceptance records and Stripe checkout metadata. `51abfa53` **materially rewrote the quiet-hours clause in both `/terms` and `/sms-terms`** on 08-31 (from "typically 8:00 PM to 8:00 AM" to "between 8:00 AM and 9:00 PM… pursuant to FCC TCPA regulations") with **no version bump** — the last touch to `terms.ts` was an unrelated auth fix. No prior version of any legal page is archived anywhere. `VALID_TERMS_VERSIONS` accepts four historical strings, so an account can hold an acceptance three revisions old and never be re-prompted. **"What did this account actually agree to" is unanswerable for every acceptance row in production**, and chargeback evidence is due in days.
- **Run it:** per-state determinations committed to `docs/`, a state gate in code (refuse generation for unmapped states), the tracking-number write or the button's removal, deletion of the surcharge modal, and — for terms — hash the rendered text of `/terms`, `/sms-terms`, `/privacy`, `/dpa` into a committed fixture so a change without a version bump goes red, plus an archived copy per version (mirror the sha256-of-consent-text pattern already at [base-plan-subscription-entrypoint.ts:264](../src/lib/billing/base-plan-subscription-entrypoint.ts#L264)).
- **Effort / who:** days. **Needs a lawyer, not an agent**, for the state determinations.

#### D7. Privacy reconciliation: egress, the Gemini no-train tier, and the policy this product writes for every contractor `[partly carried]`
- **Not covered because:** the 08-30 sweep's four named defects are all **fixed** — but a fix is not the audit, and it covered only LGQ's own subprocessor list. Not covered anywhere: (1) **the Gemini tier.** [privacy/page.tsx:115](../src/app/privacy/page.tsx#L115) publicly states AI providers operate "under commitments not to train public foundation models on customer business data", while full homeowner records (name, phone, email, address, notes) go to Gemini from [ai-assistant/engine.ts](../src/lib/ai-assistant/engine.ts) and [sms-owner-field-worker.ts](../src/lib/sms-owner-field-worker.ts). That guarantee holds **only on the paid tier**, and `grep -rni gemini docs/*.md LAUNCH_CHECKLIST.md` returns only the sweep's own *request* for the answer — never the answer. (2) **The generated contractor policy.** [legal-copy.ts](../src/lib/legal/legal-copy.ts) publishes a Privacy Policy *and* Terms under **each contractor's own name on their own domain, default-on**, naming only "AI processing providers (OpenAI)" when `@google/genai` is the sole AI SDK dependency, omitting RentCast entirely though it receives homeowner street addresses, and asserting "We do not sell your personal information" on sites where the contractor may configure a Meta pixel. Its only guard is `expect(doc).toMatch(/OpenAI|AI assistance/i)` — a substring OR-check that passes on an incomplete list. (3) **No egress reconciliation test** exists in either direction; no `playwright.config.*` exists, so no request capture on a token page has ever been possible. (4) `/letsgetquoted.com` has no Notice at Collection, no "Do Not Sell or Share" link, no GPC handling, and no financial-incentive notice for the $50 referral credit.
- **Harm:** the contractor is the controller and inherits a misstatement they never wrote, starting at website publish during onboarding — before the first invoice. And if `GEMINI_API_KEY` is on the free tier, `/privacy` carries a false consumer-facing statement that attaches to the first homeowner who fills in a contractor's form.
- **Run it:** (1) operator opens Google AI Studio / Cloud Console billing for the project owning `GEMINI_API_KEY` — PASS = billing-enabled. Record the answer *beside* `privacy/page.tsx:115`. (2) `test/privacy-egress-reconciliation.test.ts` building the outbound host set from `fetch(` literals plus the `connect-src` array in [csp.ts](../src/lib/csp.ts), asserting **both directions** against `/privacy` **and** `legal-copy.ts` — verify the second half by temporarily adding a fake host. (3) Playwright request capture on `/pay`, `/portal/view`, `/invoice`, `/quick-stop` asserting zero requests to googletagmanager / google-analytics / doubleclick / connect.facebook.net.
- **Effort / who:** half day. **Operator** for the Gemini tier.

---

## First week of real customers

Grouped by cluster; each bullet is a distinct audit.

### 1. Finish tenant isolation
- **Suspension lifecycle against a live PostgreSQL** `[carried]` — the migrations were applied with real runtime detonations (`7fcdb8aa`), but the standing regression gate is `expect(MIGRATION).toContain('a.suspended_at is null')` over **file text** and stays green on a database where nothing ran; the behaviour test uses a hand-rolled Supabase mock. Write `scripts/verify-suspension-lifecycle.mjs` on the `verify-tenant-isolation.mjs` skeleton (rolled-back transaction, `set local role authenticated`). Also decide and write down whether `suspendAccountAction` should revoke sessions — today it does not, and `getClaims()` means a ban takes up to one token lifetime to bite. *half day, repo-only*
- **All-role and write-side isolation matrix** — [verify-tenant-isolation.mjs:34](../scripts/verify-tenant-isolation.mjs#L34) is `DEFAULT_TABLES = ['clients','leads','jobs']`, filters `where role = 'owner'`, and every probe is `select count(*)` — **read-only**. Office and crew seats are on sale; no role other than owner has ever been tested, in any table, by any script, and **write-side isolation has never been probed at all** — because a `for all` policy governs SELECT too, an over-broad office write policy is invisible to a count-only prover. 20 office capability migrations postdate the last isolation audit. Run on the local PG17 harness (production has no office/crew rows — that is *why* this has never run): two accounts × 8 identities × all 61 policied tables × SELECT/INSERT/UPDATE/DELETE, and **copy the existing `vacuous` counter** — an account with no neighbour rows is not a pass. *days*
- **GRANT surface: the 13 remaining anon-executable SECURITY DEFINER functions** — the enumeration is done (Advisor: 15 anon-executable, 25 authenticated-executable, 4 mutable search paths) and two have an **uncommitted, unapplied** fix. A SECURITY DEFINER function with anon EXECUTE runs as its owner and bypasses RLS by construction. Triage the rest, then write `scripts/verify-grants.mjs` and wire it into CI **unpiped**. *half day*
- **Internal adversarial auth sweep** — one operator, two throwaway accounts, working from the manifest in B3 and the token inventory in D4: invoke every server-action id from the built bundle with no session; reach every `/admin` route as a non-staff user; substitute account B's UUID into every request body/param/path segment; join B's Realtime topics; list B's storage prefixes; replay expired and used tokens. Written result per attempt. *days* — (a **commissioned external pentest** is tracked in §12 and cannot gate launch; see Can wait.)

### 2. The AI Operator wave (2,090 lines, zero audits, shipped `c63de148`)
- **Does anything it claims to do produce a write?** [tools.ts:448](../src/lib/ai-operator/tools.ts#L448) `trigger_contractor_lifecycle_nudge` — declared as "Sends an automated onboarding or re-engagement nudge email/SMS" — calls `recordOperatorAudit` and returns `{success:true, dispatchedAt}`. **No Resend call, no SMS call, no database write.** `resolve_hitl_action` only mutates an in-memory card; no dispatcher keyed on `actionType` exists anywhere. The cockpit renders a button captioned **"✓ Approve & Execute"** on cards like "Recover High-Value Payment: $X" and the card disappears as though it happened. The one covering test asserts the stub's own returned literal. **Deliverable:** a table of promise | code path | observable artifact (Resend id / SMS row / DB row / Stripe object) for all ten tools — anything with an empty artifact column gets deleted from the declaration *and* the UI caption. *half day*
- **HITL queue and audit-trail durability** — [audit.ts:10-12](../src/lib/ai-operator/audit.ts#L10) keeps both the audit trail and the approval queue in **module-level in-memory stores**, with a comment saying they "can be backed by" tables that `grep -rn ai_operator migrations/` proves **do not exist**. On Vercel, `page.tsx` reads one lambda's memory while `resolveHitlActionServerAction` looks the id up in another's, returning `{success:false, error:'Action request … not found.'}` — which `handleResolveAction` **swallows silently** (it only filters the card out `if (res.success)`). A page headed "Autonomous Audit Trail" will read "No audit logs in current session" on essentially every load. Decide persist-or-rename **before** the console is used on a real account; a durable trail cannot be reconstructed retroactively. If persisting, the migration must `REVOKE ALL … FROM anon` and enable RLS, asserted by a test. *half day*
- **`/admin/operator` permission, MFA and nav visibility** — [page.tsx:10](../src/app/admin/operator/page.tsx#L10) is bare `requireAdmin()`, which admits **any active staff row of any role**, then renders platform MRR, dunning, paused payouts and open Stripe disputes. `requireMfaPermission` exists and is used elsewhere; this page uses neither. [AdminNav.tsx:31](../src/app/admin/AdminNav.tsx#L31) adds "AI Operator ⚡" with **no `permission` key** while `/admin/staff` carries one — so the link renders for every role. Neither file is touched by the in-flight fix. *hours*
- **LLM tool authority: the self-approving HITL** — the allowlist is fine (the `switch` default refuses unknown tools) and cross-account read is the intended capability for a staff console. What is unexamined: `validateActionExecutionSafety` is called in **exactly one** of ten branches ([tools.ts:452](../src/lib/ai-operator/tools.ts#L452)). `create_hitl_action_request` and `resolve_hitl_action` are **both model-callable**, so one model turn can mint a high-impact card and approve it. The system prompt asserts "High-impact mutations MUST be queued as HITL" — enforced by prose only. Harmless only while approved cards do nothing; the moment a dispatcher is wired, the prompt is the sole guard on refunds and trial extensions. *half day*
- **Briefing data provenance** — `a7cf16a6` already fixed most of this; three fabrications survive at HEAD: `smsDeliverabilityPct: failedSms.length === 0 ? 100 : 98.5` (an invented constant, not a rate); `PLAN_MRR_WEIGHTS` duplicating `BILLING_PLANS` with `test/ai-operator.test.ts` pinning `toBe(930)` from fixtures, so **a catalog price change leaves the test green and the number wrong**; and the no-key fallback calling `executeOperatorTool('diagnose_contractor_onboarding', { accountId: 'acc-test-123' })` — a real tool against a fabricated id, rendered to the founder under "**Onboarding Diagnostics**". *half day*
- **Test-suite bite** — 504 lines covering ~1,870 lines of new server code that reaches Stripe, Supabase and Gemini, and it is the evidence anyone will cite. No test asserts an authorization refusal on any operator action, so removing a guard stays green. And [test/ai-operator.test.ts:499](../test/ai-operator.test.ts#L499) is **environment-dependent**: it expects a tool call that `engine.ts` produces only on the `if (!apiKey)` fallback, and nothing in `vitest.config.ts` clears the ambient `GEMINI_API_KEY` — so on any machine or CI runner with the key exported, the suite makes a **live billed network call to Google** and its assertion becomes model-dependent. *hours*
- **Support-copilot drafted replies** — at committed HEAD, [support-copilot.ts:204](../src/lib/ai-operator/support-copilot.ts#L204) tells customers "a dedicated local SMS number … 100% white-labeled" (every plan grants **0**) and :279 "SSL certificates are issued automatically" (custom domains fail TLS). Both are reachable from the LLM console via `triage_support_case`, and `diagnoseContractorOnboarding` has two production callers — the item's original "zero callers" claim was wrong in the *more* reachable direction. A human pastes these, which is why it is first-week rather than a blocker. Also fix the jobs/quotes conflation (quote counts derived from the `jobs` table, the same defect as `contractor-lifecycle-emails.ts:453`). *hours*
- **Gemini/Google credential presence in Production** `[unknowable from repo]` — four call sites branch on the key, and two are customer-visible defects regardless of what Production holds and are fixable today: [ai-assistant/engine.ts:204](../src/lib/ai-assistant/engine.ts#L204) renders to a **paying contractor** the literal string "please configure `GEMINI_API_KEY` in your `.env.local` file"; and the vision route returns a fabricated all-clear (below). The console half decides which of two materially different products launched — and Vercel bakes env at build, so a key added after `e3550f58` is inert. *hours, operator*
- **The Gemini vision endpoint** — [lead-photos/ai-suggest/route.ts](../src/app/api/lead-photos/ai-suggest/route.ts) went from placeholder annotations to a live `gemini-2.5-flash` call uploading homeowner property photos to Google, with **no rate limit** (its sibling shipped in the same wave has one), **no AI-credit metering** (`ai_intake_100` and `ai_writing_250` are sold SKUs; Flex advertises 55 AI credits; this route debits nothing), and a no-key branch returning `{success:true, summary:'AI vision engine ready. No automated defects detected.'}` — **a clean bill of health for a photo nothing looked at**. The sharpest fact: `51abfa53`, the commit that *remediated* the post-sweep audit's "fabricated defect spans" P0, is the commit that rewrote this file and left that branch in. (The cross-tenant leg is refuted — reads go through `createSignedUrls`, so a bare foreign path gets a 403 from Storage.) *half day*

### 3. Edge and browser posture
- **CSP enforcement state, and the tripwire that was removed with it** — [csp.ts:108](../src/lib/csp.ts#L108) is `CSP_REPORT_ONLY = true` (set by `6ef05c6a`), while ~100 lines of comment above it are still headed "NOW ENFORCING (2026-08-03)". The diagnostic half is done and tracked (§6: nonce reaches 0 of 39 homepage scripts). **What nobody knows:** the same two-line commit changed [test/csp.test.ts:182](../test/csp.test.ts#L182) from `expect(CSP_REPORT_ONLY).toBe(false)` to `expect(typeof CSP_REPORT_ONLY).toBe('boolean')` — inside a test still **named** `it('enforces, and the header name follows the flag')`. The remaining assertion is a tautology over the same constant. The site can ship permanently report-only and stay green. `frame-ancestors` **is** enforced via [next.config.mjs:33](../next.config.mjs#L33), so clickjacking is genuinely covered; `script-src`, `connect-src`, `form-action`, `base-uri` and `object-src` are enforced nowhere. Fix the root cause (C1), then restore `toBe(false)`. *days*
- **The CSP report pipeline has no consumer** — [csp-report/route.ts:57](../src/app/api/csp-report/route.ts#L57) is `console.warn` and nothing else; no alert, no dashboard, no cron, no named reader, and the last written instruction ("Watch `[csp-report]` in the logs") has no owner. Report-only is defensible only as a temporary state **with an exit condition**, and there is none. Only deprecated `report-uri` is emitted — no `report-to`, no `Reporting-Endpoints`. Cost nobody has priced: each report POST costs one Supabase `check_rate_limit` RPC, and the checklist measured 39 unnonced scripts per homepage load — right as a Google Ads campaign starts sending strangers there. *half day, operator for the log query*
- **Session cookie flags** — `@supabase/ssr` 0.5.2's `DEFAULT_COOKIE_OPTIONS` is `{ path:'/', sameSite:'lax', httpOnly:false, maxAge: 400 days }` with **no `secure`**, and both construction sites ([middleware.ts:126](../src/middleware.ts#L126), [supabase-server.ts:16](../src/lib/supabase-server.ts#L16)) pass options straight through. So the session token guarding a workspace's Stripe payout settings and every client's contact details is readable from `document.cookie` for up to 400 days — and the compensating control that makes that acceptable, an enforced `script-src`, was turned off yesterday. `httpOnly:false` may be forced by the browser client, in which case the deliverable is a **recorded decision naming enforced CSP as the compensating control** — which makes the CSP item a hard dependency. Setting `secure: true` on every app-controlled cookie is unconditional and cheap. *half day*
- **`@supabase/ssr` 0.5.x → 0.12.x** `[carried]` — [package.json:59](../package.json#L59) is still `^0.5.0`, unchanged across the frontier range, on a package that **predates Next 15 entirely** and now runs against an async cookie API it was never released for. Production has been serving authenticated sessions on 15.5.24 since `e152015c`, so the "random logouts" harm is largely refuted — what remains is the changelog review and the cookie posture. Do not bump blind; drive a real browser through magic link, hard refresh, middleware redirect, server action, expiry/refresh, plus the crew `/field` and office-invite callbacks. *days*
- **Security headers as actually served** — `grep -rln 'Strict-Transport-Security|X-Frame-Options|SECURITY_HEADERS' test/ scripts/` returns **NOTHING**: no test pins the source values, no script probes production, and `vercel.json` has no headers block. The config's own comment says HSTS `includeSubDomains` is "load-bearing, not decorative: every contractor site is a subdomain" — and nothing checks it arrives on one. Ten-minute curl per host class (apex, app, tenant subdomain, `/api/health`, a static asset, `/manifest.webmanifest`), plus a test pinning the source and a script pinning the deploy. Decide COOP/CORP explicitly (Turnstile, Maps and the YouTube embed are cross-origin; COEP would break them). *hours, operator*
- **Middleware matcher coverage** — one regex, [middleware.ts:311](../src/middleware.ts#L311), decides four separate things (CSP header, canonical-host redirect, tenant rewrite, `x-pathname`) and **nothing tests it**. `/api` gets no canonical-host redirect, on a codebase whose own comment says "a session cookie belongs to exactly one host". The lookahead is prefix-based, so `/apifoo` also escapes. On a tenant subdomain, `/sw.js` is rewritten to `/site/<sub>/sw.js`, which cannot resolve — and the PWA registers it. Write the per-request-class table, curl it, and pin `config.matcher` with a ~15-path test. *half day*
- **Client-bundle secret scan against the BUILT output** `[stale — re-run]` — the last clean verdict is 2026-08-22, **520 commits and one framework major ago**, and was explicitly carried forward rather than re-checked by the post-sweep audit. Static evidence at HEAD is clean and I verified it, but **no script or test ever inspects `.next/static`**, and the static checks cannot catch a secret passed as a **prop** from a server component into a client component — serialized into the RSC payload, invisible to `test/client-server-boundary.test.ts`, which only walks the import graph for `server-only` markers. The tree holds a real `sk_live_` key and a service-role JWT. `NEXT_DIST_DIR=.next-scan npm run build`, grep for key prefixes *and* the literal values, decode every JWT-shaped string and assert none has `"role":"service_role"`, wire as `verify:bundle` after Build, and prove it bites with a planted canary routed through a prop. *hours*
- **Next 15 caching-default inversion** — `grep -n 'staleTimes|experimental' next.config.mjs` returns **nothing**, so the client Router Cache staleTime for page segments takes Next 15's default of **0** (down from 30s), and every back/forward in the dashboard refetches the RSC payload — while the only performance number anyone quotes (−58%) was measured on Next 14 and was never re-measured even then. The opposite direction has a documented victim in this codebase (a claim RPC returning the same row ten times through the patched global fetch that supabase-js uses). `288f7f3a` touched **no fetch call site**. Make `staleTimes` explicit even if you choose 0, then re-measure with `responseEnd` (TTFB is a flat ~14ms streaming lie; there is an ~830ms auth floor). *days*

### 4. Operations maturity
- **`/api/health` truthfulness + an external uptime probe** — three of four services report "operational" from **env-var presence alone**: `Boolean(SIGNALWIRE_PROJECT_ID && SIGNALWIRE_API_TOKEN)` → "SignalWire / 10DLC Carrier Network operational", `Boolean(STRIPE_SECRET_KEY)` → "Stripe Connect API V2 operational", `Boolean(NEXT_PUBLIC_ROOT_DOMAIN)` → "Global Anycast Edge Network operational". They read green through a total Stripe outage — in a commit titled "fail-closed alerting". This repo already has a live 10DLC assignment failure and a rejected campaign. The endpoint is consumed by [HelpCenter.tsx:532](../src/components/help-center/HelpCenter.tsx#L532) and shown to signed-in contractors, and that call guards on `if (res.ok)` while the route returns **503 during an outage** — so the customer status modal renders blank precisely when it matters. `HelpCenter.tsx:1980` also hardcodes "Google Cloud Run (us-east1)" on a Supabase/Vercel stack. Nothing polls the endpoint: `uptimerobot|betterstack|checkly|cronitor|pingdom|pagerduty` = **0 hits** repo-wide, and there is no `/status` page. Register one 5-minute external check and prove it by taking it offline. *hours, operator*
- **Cron-health alert credibility** — beyond the IPv6 defect in A3: [inspect-cron-health.mjs](../scripts/inspect-cron-health.mjs) exits nonzero on **any** silent/stale/failing job with **no allowlist for deliberately-dark workers**, and a flag-gated cron returns 404 before writing a heartbeat (its own header comment explains this and then does not model it), so an intentionally-off worker is indistinguishable from a broken one. A missing `DATABASE_URL` secret produces the identical red. **A pager that is always red is training, not monitoring** — on the exact fleet that once ran seven crons for weeks with nobody noticing. Add a `DARK_JOBS` allowlist sourced from the flag names in `billing-worker-cron.ts`. *hours*
- **Per-cron failure-mode and replay-safety inventory** — all 35 wrap `cronRoute` and are fail-closed on `CRON_SECRET`; the money rails carry `SKIP LOCKED`/advisory locks and named PG17 replay provers. What is missing is the written record and the timeout: `grep -rn maxDuration src/app/api/cron/*/route.ts` returns **16 of 35**, and **every money worker is among the 19 without one** — so a mid-batch timeout happens against an unwritten platform default. `ad-wallet-refill` charges cards directly and its persist-key-before-charge fix was a remediated P0, which proves the class is real and that nobody has swept the other nine. One row per job: schedule, flag, maxDuration, batch size, what a crash between the external call and the DB write leaves behind, and which test proves replay is safe. *days*
- **Stripe-to-app ledger reconciliation** — nothing anywhere compares aggregate Stripe truth to app truth (`ls scripts/` has no reconcile target; `verify-refund-reconciliation.mjs` is a migration-contract test). The only platform revenue figure anyone sees, [briefing.ts](../src/lib/ai-operator/briefing.ts) `calculatePlatformRevenueMetrics`, **never consults Stripe** — and its primary branch reads `workspace_entitlements`, a table with **no `test_marker` column**, while both fallbacks filter on it. Preview writes to the production Stripe account, so the headline MRR will count rehearsal subscriptions as revenue. Write a read-only three-column diff (Stripe only / app only / amount mismatch) for a date range. *half day, operator*
- **Webhook authenticity: adversarial probe of all six endpoints as a set** — the implementations are genuinely good and real negative *unit* coverage exists per endpoint, and both live Stripe endpoints are console-verified. What has never happened is a **live** forged, wrong-secret, replayed, or oversized delivery. Two structural gaps stay open: app-level body limits, and the fact that signature-failure branches write 500-byte attacker excerpts to `webhook_failures` with no dedup while the admin reader shows only the newest 50 — a stranger can page every real failure off the dashboard. Also fix the posture gate: those four routes pass on the string "webhook" in their path. *half day, operator*
- **Secret-absence fail-mode matrix + one rehearsed rotation** — behaviour on absence is inconsistent and written down nowhere: [rate-limit.ts:30](../src/lib/rate-limit.ts#L30) fails **open** while :46 fails **closed** in the same file; [contact/actions.ts:32](../src/app/contact/actions.ts#L32) **skips captcha entirely** with a `console.warn` when `TURNSTILE_SECRET` is unset; `email.ts` has 19 silent-skip sites *and* throwing sites for the same key. (The invoice-email skip is a known, intended Preview-only state — documented in the file.) Because Sensitive vars are unreadable, nobody can state which condition the app runs under right now. Separately: no rotation procedure exists, and rotating the Supabase service-role key would kill five HMAC rails at once (D4) — rehearse **one** low-risk rotation end to end (add new → redeploy → verify traffic → revoke old → verify old fails) and run `gitleaks detect --log-opts=--all` over full history. *half day each, operator*
- **Incident forensics: log retention** — `cron_runs` keeps 90 days and prunes probabilistically; staff actions are durably audited; the AI Operator's action log is erased on cold start; application runtime logs go to Vercel only and **the retention window on the current plan is not knowable from the repo**. Pick one payment from last week and reconstruct its full lifecycle from Postgres alone. (The cardholder-PII logging this item originally cited is already fixed — the webhook now logs only `{code, decline_code, message}`.) *hours, operator*

### 5. Scale
- **2,000-row seeded workspace + connection-pool headroom** `[carried]` — nothing has exercised this app above ~100 seeded rows or one concurrent session; no k6/artillery/autocannon anywhere. The truncation defect it was meant to expose is **partly** fixed (`listJobs` now takes `fetchAll`, and both CSV exporters pass it), but every other caller — the dashboard job list, the scheduler — silently stops at Supabase's 1,000-row ceiling, presenting as a short list rather than an error. Pool headroom under 35 crons plus real sessions is unmeasured. Single-user perf **has** been measured (production mobile synthetic: homepage LCP 5.95s median); concurrency has not. Measure `responseEnd`, not TTFB. **Any page that renders exactly 1,000 rows is a failure regardless of speed.** *days, operator*

### 6. Regulated conduct with a slower clock
- **Call-recording disclosure ordering** — the AI-identity announcement **does** exist and fires unconditionally, so `/terms:119-124` is honoured on that half; the original claim to the contrary is wrong. What is broken is the ordering: [voice/signalwire.ts:192](../src/lib/voice/signalwire.ts#L192) puts `record_call` at SWML `main[1]` and the disclosure play at `main[2]` — **the first recorded audio predates the announcement** — and [test/voice-calls-workspace-e2e.test.ts:143](../test/voice-calls-workspace-e2e.test.ts#L143) **asserts that ordering as correct**, so the guard vouches for the defect. In an all-party-consent state that is the whole offense. No per-state analysis exists. AI Voice is not purchasable, so this does not gate the first subscription. *half day*
- **Employee-monitoring notice** — `crew_location_state` durably records lat/lng, heading and speed for a worker whose only consent artifact is the browser's OS geolocation prompt; the `canShareLocation` toggle at [CrewRoster.tsx:1789](../src/app/dashboard/crew/CrewRoster.tsx#L1789) belongs to the **owner**, not the worker. `/terms:90-95` assigns the notice duty to the contractor and the product gives them **no mechanism to discharge it** — grep across `src/app/field` and the crew onboarding returns no crew-facing notice anywhere. The crew member is invited by SMS, and `ensureSmsConsentBaseline(...).catch(() => {})` auto-creates consent for their personal phone with no action on their part. Several states require written pre-monitoring notice. Triggered by the first crew member the first paying contractor adds. *days*
- **Accessibility legal posture (distinct from the tracked WCAG remediation)** — `find src/app -iname '*accessib*'` returns nothing; no statement page, no feedback route, no VPAT/ACR, no exposure determination. The homeowner token surfaces are consumer-facing by construction — a homeowner receives an SMS link and has **no alternative path to pay an invoice**. Note: the measured defect list an honest ACR would cite (Quick Stops hydration mismatch, Schedule console error, 25 sub-24px targets, a collapsed `aria-controls`) was **deleted from the working-tree checklist** under a still-ticked axe line, so it now survives only in git history — which strengthens the case for writing the ACR now. A candid ACR with a remediation timeline is the artifact that has value; a clean one is not. *half day*

### 7. Standing gates (prevent the next regression rather than catch a live one)
- **Three-way reconciliation: SKU ↔ live Stripe Price ↔ what production actually grants** — legs 1 and 2 were verified by hand on 2026-08-31 and nothing re-verifies them; `ci.yml` runs neither `preflight:prices` nor `inspect:live-top-ups` nor `audit:applied`. **Leg 3 has no reader at all:** `test/allowance-grant-tables-match-catalog.test.ts` parses migration *text*, and the PG17 scripts run against a scratch database — nothing calls the grant function **installed in production**. The precedent failure was Scale silently granted Growth's allowance on a $329 plan, forever, with nothing erroring. *days, operator*
- **Plan comparison matrix** — 20 rows × 4 columns; ~15 cells plus the whole price book are pinned by existing tests, and the rest are not, including "Custom-domain connections: 1" on all four plans (a rail that cannot serve TLS) and "5/10/100/250 GB storage" with `LGQ_STORAGE_CAP_ENFORCED` off. Derive from `BILLING_PLANS` or iterate all four columns with an explicit prose-row allowlist. *days*
- **Money-format invariant derived from the filesystem** — the guard is a hand-maintained 9-file list whose own header records being wrong **twice** (once naming pages instead of the components they render, once missing the invoice email and its PDF). `test/invoice-pdf-render.test.ts` asserts only a PDF magic header, page count and no-throw — **the rendered-output leg does not exist**. New unguarded surface: [CancelSubscriptionPanel.tsx:51,82](../src/app/dashboard/settings/CancelSubscriptionPanel.tsx#L51) rolls its own formatter for the guarantee refund (correct today; unpinned). Derive the list, and extract text from one $4,237.50 fixture through page, email body and PDF. *days*
- **Compliance-copy claim register** — no test reads `/terms` or `/sms-terms` against behaviour; the 1099-K threshold is not pinned to a constant; no test asserts every outbound host appears in the privacy page. Give each legally operative sentence an id and a mechanical assertion. **Note a live contradiction to settle:** §0 ticks quiet-hours durable delivery as remediated while §3 keeps "Quiet-Hours Delivery Contract" open — and the new persistence step is a best-effort `available_at` update inside a try/catch, enqueued **first** and stamped **after**, so the worker can claim the row in the window between, and a zero-row update reports success. *days*
- **Dead-feature detector** — `ALL_FEATURES_CATALOG` publishes **132 features** rendered publicly, referenced by **zero** test files; `test/changelog.test.ts` is shape-only. Four features have been sampled by hand and **all four were dead** (`neighborhood-halo-ai`, `satellite-property-sizing`, `ad-closed-loop-sync`, `neighborhood-halo` — zero non-test importers), two carrying `favorite:true` so they appear in the homepage highlight band. The base rate across the other 128 is unknown. *days*

---

## Can wait

| Audit | Why it waits |
|---|---|
| **Magic-link single-use / enumeration probe** `[carried]` | [magic-link.ts:25-33](../src/lib/magic-link.ts#L25) throws on `generateLink` for an address with no user, plausibly making unknown emails distinguishable — but `login/actions.ts` already rate-limits per-email and per-IP with a fail-closed limiter, capping the enumeration rate. Cheap (hours) and worth doing; low severity next to the money and alerting items, and there is nobody to enumerate yet. |
| **Transactional hard-bounce policy** `[carried, "Later" bucket]` | The classification machinery works — [email-suppression.ts:125](../src/lib/email-suppression.ts#L125) returns `hard_bounce` on a permanent bounce and the webhook records it — but not one transactional sender consults it (all five `isEmailSuppressed` callers are marketing or UI). Correct by design for a marketing unsubscribe; the missing piece is a *separate* `hasHardBounce` predicate. With no customers there is no bounce volume to damage the domain. The one auditable part now is inside B1: does `email_events` have any rows at all. |
| **Commissioned external penetration test** | Tracked verbatim in §12. A scoped engagement cannot complete before launch; make it a **dated line item with an owner**, not an unchecked box. The internal sweep (first week, cluster 1) is the pre-launch substitute. |
| **`schema.sql`-vs-production parity probe** `[carried, "Later"]` | No database-to-database diff exists anywhere (all four candidate scripts are offline/file-to-file/embedded-PG). It bites the day you need to rebuild — so attach it to the restore drill (A2) rather than tracking it separately. |
| **Next 15 Router Cache re-measurement** | The `staleTimes` decision belongs in first week; the −58% re-measurement can follow. |
| **Push-notification content policy** | Homeowner names on crew lock screens ([jobs/actions.ts:647,754](../src/app/dashboard/jobs/actions.ts#L647)). Already examined and classified by the 08-30 sweep as a deliberate UX tradeoff worth confirming; transit is E2E-encrypted. A product decision and a 10-line change, not an audit. |

---

## Audits already run and current

Listed so that the **absence** of an item above means something.

**Live and dated (2026-08-31 unless noted):**
- **Production dependency audit** — `npm audit --omit=dev` exit 0, 0 vulnerabilities across 187 production dependencies. The 5 remaining advisories are dev-only (vitest/vite/esbuild) and never ship.
- **Live RLS baseline** — 162 of 162 public tables have RLS enabled; no browser-reachable table lacks it; both views use `security_invoker`; anon/authenticated cannot create objects in `public`.
- **Supabase Security Advisor run** — enumerated: 15 anon-executable SECURITY DEFINER functions, 25 authenticated-executable, 4 mutable search paths. *(Triage of the 13 remaining is first-week.)*
- **Live top-up price contract** — all 6 sellable SKUs `contract-ok` against their live Stripe Price via `inspect:live-top-ups`, which asserts `unit_amount`, `recurring`, `livemode`, currency, tax behavior and all five metadata fields. **The dollar labels beside those Price IDs in the checklist are a prose transcription error, not a live mismatch.**
- **Live Stripe webhook endpoints** — both primary endpoints verified via the API: subscribed events, delivery counts, 0 failed, secret present. *(Three of five receivers remain unverified — see A1.)*
- **Live base-plan subscription checkout, end to end** — Solo Monthly, real live events ingested and projected to `subscription_and_invoice_state_applied`, entitlement granted. *(Real; the checklist's date is wrong — it happened ~08-23.)*
- **Live subscription cancellation at period end** — 2026-08-29, `account_events` + webhook ingested at 1.0s latency.
- **Live connected-account charge + refund** — 2026-08-17, $0.50 captured and refunded, platform fee reversed. *(Refunded from the Stripe dashboard, so the app's refund code was not exercised.)*
- **Live owner read isolation sample** — seven production owners, own rows only.
- **Custom-domain enumeration** — production has **zero** configured custom domains, which is why the TLS failure has never bitten a customer.
- **Cron fleet health** — 33 of 35 healthy in a strict 24-hour production audit.
- **Production runtime log review** — a 24-hour sample; no runtime errors observed after `e3550f58`. *(Point-in-time, not continuous.)*
- **Edge routing** — app login and unauthenticated dashboard→login verified against the Next 15 deployment.
- **Migration application** — `audit:applied` reports 0 gaps; the 8-migration wave was applied 2026-08-30 after 12 runtime defects were detonated in rolled-back transactions and fixed.
- **Build / typecheck / lint at Next 15.5.24** — all exit 0 locally with recorded times, 386/386 static pages. *(Never in CI — see A4.)*

**Standing gates that genuinely bite:**
- `test/env-example-covers-what-the-code-reads.test.ts` — walks all of `src` (with a >500-file floor), extracts `process.env.X`, bracket access **and** bare quoted `LGQ_*` literals, and asserts each is an *assigned key* in `.env.example`, with a named regression test proving a longer name cannot vouch for a shorter one.
- `test/catalog-version-is-a-data-migration.test.ts` — pins the two-halves rule (widen EVIDENCE readers, **move** CURRENTNESS rows) at the point of edit.
- `test/pricing-add-ons-are-buyable.test.ts` / `test/ai-voice-foundation.test.ts` / `test/crew-purchased-capacity-migration.test.ts` — withheld SKUs cannot be purchased; the entrypoint refuses with `sku_withheld`, the projector blocks `fulfillment_withheld`.
- `test/google-tag.test.ts` — a real behaviour test of `isSensitivePath`, including the fail-closed branch.
- Per-endpoint **negative** webhook unit coverage — missing secret, reused secret, missing signature before body read, duplicate acknowledgement, unreadable body; plus a valid *Twilio* signature rejected at the SignalWire boundary and expired/forged SWAIG tokens.
- `test/payment-actions-are-guarded.test.ts` — asserts the guard is **called**, not merely imported. This is the technique the route/action manifest should copy.
- `test/payments-advanced-enhancements.test.ts` — pins the corrected 1099-K wording **and** asserts the old categorical claim is absent.

**Remediated and verified since the post-sweep audit:** the lead-photo proxy SSRF; cardholder detail no longer logged from `last_payment_error`; the Google Ads tag's sensitive-path guard; the `ad-wallet-refill` persist-key-before-charge race; the insurance-letter attestation and sample-data separation; the strict-mode fail-closed exit in `inspect-cron-health.mjs`.

**Not a launch surface at all:** PCI card-entry. Grep for `cardNumber|cvc|PaymentElement|loadStripe|ui_mode` across `src/app`, `src/components` and `src/lib` returns **zero** real hits — every payment is redirect-to-hosted. Selecting and attesting an SAQ is first-quarter paperwork, not a gate.

---

## Cannot be answered from the repo — needs the operator or a human

| Question | Where | Blocks |
|---|---|---|
| Do `GEMINI_API_KEY` / `GOOGLE_API_KEY` exist in Production, and was the current deployment built **after** they were added? | Vercel | D7, AI cluster |
| Is the Gemini project on the **paid** tier (the no-train commitment)? | Google Cloud / AI Studio | D7 — a public legal claim |
| Names + `createdAt` for all 64 `LGQ_*` flags, `RESEND_WEBHOOK_SECRET`, `COMPANY_MAILING_ADDRESS`, `STRIPE_TOP_UP_WEBHOOK_SECRET` | `vercel env ls production` | A5, B1, C4 |
| Supabase billing tier, PITR state, backup retention | Supabase console | A2 — substantiates a public claim |
| Does the project **enforce private Realtime channels**? | Supabase console | D1 — live crew GPS |
| `storage.buckets.public` for all 7, and every `storage.objects` policy | Supabase SQL | D2 |
| Supabase Auth SMS rate limits and spend caps | Supabase console | B4 |
| `stripe webhook_endpoints list --live` for **both** accounts, all five receivers | Stripe API (never the dashboard UI) | A1 |
| Live execution: app-issued refund, top-up purchase, plan change, dispute | Stripe live + a real card | A1 |
| Is the Resend production webhook registered, and does `letsgetquoted.com` root publish `v=spf1`? | Resend console + registrar | B1 |
| Are the three GitHub repo secrets set, and does the failure email reach a human? | GitHub | A3 |
| Vercel log retention on the current plan | Vercel | Forensics |
| Do `support@` and `hello@` deliver to a monitored human? Does `dmarc@` receive? | Mailboxes | A7, B1 |
| Is the Google Ads conversion actually recording? | Google Ads | C1 |
| **Lawyer, not an agent:** lien/NOI validity per state, public-adjusting exposure, surcharge legality, state privacy rights, ADA posture, all-party-consent recording, employee-monitoring notice | — | D5, D6, first-week cluster 6 |

---

## Carried over unrun from the 2026-08-30 sweep — a day later, still not done

**11 items.** Backup + restore drill *(the sweep called it the worst finding)* · failure-to-human alert drill · live-mode Stripe money rehearsal · email deliverability · abuse-surface rate-limit allowlist · homeowner mobile matrix · clean-slate onboarding walkthrough · support-channel probe · suspension-lifecycle test · marketing-compliance invariant test · offsite restore rehearsal. Plus, correctly deferred: `@supabase/ssr` upgrade, scale probe, magic-link probe, hard-bounce policy.

Everything else in this document is **new** — needed because of the AI-operator wave (`c63de148`), Next 14→15.5.24 (`288f7f3a`/`e152015c`), CSP report-only (`6ef05c6a`), the email template rewrite (`f8969fb0`), the mandatory contact-preference field (`b783bfce`), the cron-staleness workflow (`5e57e2e8`/`0df04834`), or the P0 remediations themselves (`51abfa53`, `51143e76`, `08cdd98a`, `e3550f58`, `cd5c28b0`) — two of which **introduced** findings while closing others.

---

## Confidence

**High** (re-verified independently, usually twice, against committed HEAD): the Next-15 render-mode chain and its three consumers; the CSP flag and the neutered test (`git show 6ef05c6a -- test/csp.test.ts`); the posture-test disjunction and the 128/13 route counts; the 400 `createAdminClient` sites; `private: true` = 0 hits; zero `storage.objects` policies; the cron-health workflow's 7-for-7 failure record; the contractor-lifecycle step loop; the fail-open suppression reads; `Math.random()` in crew verification; the terms-clause rewrite without a version bump; the surcharge modal; the AI-Operator in-memory stores and the no-op nudge tool.

**Medium — single agent read, or outside the repo:** the `D:\dev-backup` rail's contents and log gaps (verified by one agent on the filesystem; the *repo-side* claim that nothing in git does backups is confirmed). The Vercel/Stripe/Supabase console states are asserted from the checklist, which this project's own memory records as certifying falsehoods — treat every one as a claim.

**Conditional — the harm depends on a console setting nobody has read:** D1 (Realtime) and D2 (Storage). The repo proves the code does not *request* a private channel and declares no storage policy; only the console can say whether the project enforces one anyway. Both are ranked as blockers because the downside if the setting is permissive is the worst in the product, not because a leak is demonstrated.

**Moving target:** HEAD advanced during this audit and the working tree differs from committed HEAD on the AI-Operator authorization fix (written, uncommitted, undeployed), `LAUNCH_CHECKLIST.md` (160 changed lines, including the **deletion** of the candid accessibility defect list), and `ad-speed-to-lead.ts`. **Production is `e3550f58`, five commits behind local HEAD.** Freeze a SHA before running anything, and re-verify the AI-Operator items against whatever actually deploys.