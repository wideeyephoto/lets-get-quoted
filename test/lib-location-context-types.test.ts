import { describe, it, expect } from 'vitest';
import * as types from '@/lib/location-context/types';

describe('Location Context Types', () => {
  it('loads types', () => {
    expect(types).toBeDefined();
  });
});
