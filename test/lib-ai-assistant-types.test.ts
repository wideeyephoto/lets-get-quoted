import { describe, it, expect } from 'vitest';
import * as types from '@/lib/ai-assistant/types';

describe('AI Assistant Types', () => {
  it('loads types', () => {
    expect(types).toBeDefined();
  });
});
