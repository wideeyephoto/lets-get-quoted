/* The public site's one header — see the component for why this is a layout
   and what has to stay in step with it (OWN_CHROME_MARKETING_ROUTES). */
import './pricing-v2.css';

export { default } from '@/components/flagship/public-header-layout';
