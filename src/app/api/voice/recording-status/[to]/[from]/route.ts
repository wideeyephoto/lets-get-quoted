// Preserve the original URL so the recording handler authenticates the entire
// attribution path with the provider's signature before accepting it.
export { POST } from '@/app/api/voice/recording-status/route';
