const fs = require('fs'); let t = fs.readFileSync('test/legacy-payment-rail-guard.test.ts', 'utf8'); t = t.replace(/eq: vi\.fn\(\(\) => (q|query)\),/g, function(match, p1) { return 'is: vi.fn(() => ' + p1 + '),\n        eq: vi.fn(() => ' + p1 + '),'; }); t = t.replace(/eq: vi\.fn\(\(column: string, value: unknown\) => {/g, function(match) { return 'is: vi.fn(() => query),\n      eq: vi.fn((column: string, value: unknown) => {'; }); fs.writeFileSync('test/legacy-payment-rail-guard.test.ts', t);
