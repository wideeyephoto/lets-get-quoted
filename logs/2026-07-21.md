# Edit log — 2026-07-21

## AI-generated example text for website builder
- Added `generateSiteTextAction` (src/app/dashboard/sites/actions.ts) that calls
  OpenAI to write randomized headline/tagline/SEO title/description tailored to
  the contractor's inferred trade, using a random tone seed + high temperature
  so two contractors don't land on identical unedited placeholder copy.
- Added a "✨ Generate example text with AI" button to the Business tab in
  WebsiteBuilder.tsx (confirms before overwriting existing text; only applies
  to local state, still requires Save).

## De-remodel-bias pass on the 3 curated templates
- Found that all 3 curated templates (Forge/carbon, Guild/professional,
  Vista/modern) had hardcoded hero/section copy written for general
  contractors/remodelers ("Built right. Built to last.", "A better way to
  build and renovate.", "Build / Renovate / Refine", etc.) — looks wrong for
  HVAC, cleaning, plumbing, electrical, etc. Rewrote the fixed microcopy in
  all 3 templates to be trade-neutral.
- Bigger bug: Guild template's "Services" section had a fully hardcoded list
  (`['Renovations & additions', 'Kitchens & interiors', 'New construction']`)
  shown on every site regardless of trade — replaced with a generic
  repairs/installs/maintenance list that applies across trades.
- Fixed HeroQuickForm's describe-step placeholder ("e.g. Kitchen remodel, new
  deck, roof replacement...") to a trade-neutral example set, and removed the
  dead/unused `hint` fields on SIZE_OPTIONS/TIER_OPTIONS that still hardcoded
  remodel-specific language ("Full room remodel").
