# Edit log — 2026-07-17

## Template quality pass (Forge / Guild / Vista)
- Fixed font loading + wiring so each of the three original templates actually
  uses its intended display/body webfonts via `--theme-display` CSS variables
  instead of falling back to system fonts.
- Reworked `quote-request-form.module.css` styling/polish (spacing, hover
  states) and touched up gallery/button hover states in `forge.tsx`,
  `modern.tsx`, `professional.tsx`, `themes.module.css`.
- Deleted `src/lib/templates/carbon.tsx` — confirmed dead code (never
  registered/imported; `registerTemplate('carbon', ForgeTemplate)` in
  `index.ts` actually backs the `carbon` id with `forge.tsx`).

## 20-template roster expansion
- Extended `TemplateType` union in `src/lib/sites.ts` with 16 new ids.
- Loaded 15 new Google Font pairings in `src/app/layout.tsx`, each exposed as
  a `--font-<id>-display`/`--font-<id>-body` CSS variable on `<body>`.
- Built 17 new templates (`.tsx` + `.module.css` pairs in
  `src/lib/templates/`) across 5 archetypes:
  - Archetype D (centered minimal, no hero image): Haven (`minimal`),
    Meridian, Blueprint, Lumen.
  - Archetype E (data/stat-driven): Atlas, Circuit, Cascade, Anchor.
  - Archetype A additions (Forge-style full-bleed hero): Foundry, Ironclad,
    Summit.
  - Archetype B additions (Guild-style split hero): Beacon, Timber, Heritage.
  - Archetype C additions (Vista-style rounded full-bleed hero): Bloom,
    Drift, Nova.
- Wired all 17 into `src/lib/templates/index.ts` (imports + `registerTemplate`
  calls) and `src/lib/templates/types.ts` (`AVAILABLE_TEMPLATES` entries with
  `previewImage` cycled from the existing 6 `STOCK_SITE_IMAGES` URLs — no
  fabricated image URLs).
- Verified: `npx tsc --noEmit` clean, `get_errors` clean across all new/
  modified files, browser spot-check of Blueprint/Nova/Atlas at
  `/themes/<id>` rendered correctly (fonts, theme colors, layout, quote form).
- Total template count: 20 (was 3).
